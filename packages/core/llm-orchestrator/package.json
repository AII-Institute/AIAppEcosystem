{
  "name": "@ecosystem/core-llm-orchestrator",
  "version": "0.1.0",
  "description": "LLM model routing and inference orchestration",
  "private": true,
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "jest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ecosystem/types": "*",
    "@ecosystem/shared": "*"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
