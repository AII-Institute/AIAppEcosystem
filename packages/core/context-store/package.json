{
  "name": "@ecosystem/core-context-store",
  "version": "0.1.0",
  "description": "Encrypted Redis-backed distributed session state",
  "private": true,
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ecosystem/types": "*",
    "@ecosystem/shared": "*"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
