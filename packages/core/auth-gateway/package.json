{
  "name": "@ecosystem/core-auth-gateway",
  "version": "0.1.0",
  "description": "OAuth 2.1 + PKCE authentication gateway",
  "private": true,
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "jest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ecosystem/types": "*",
    "@ecosystem/shared": "*"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
